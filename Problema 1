# Lista para almacenar los números que cumplen con las condiciones
numeros_divisibles = []

# Itera a través del rango de 1500 a 2700 (incluyendo ambos)
for numero in range(1500, 2701):
    # Verifica si el número es divisible por 7 y múltiplo de 5
    if numero % 7 == 0 and numero % 5 == 0:
        # Agrega el número a la lista de números divisibles
        numeros_divisibles.append(numero)

# Imprime los números que cumplen con las condiciones
print("Los números divisibles por 7 y múltiplos de 5 entre 1500 y 2700 son:")
print(numeros_divisibles)